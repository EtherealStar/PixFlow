<?xml version="1.0"?>
<!DOCTYPE module PUBLIC
        "-//Checkstyle//DTD Checkstyle Configuration 1.3//EN"
        "https://checkstyle.org/dtds/configuration_1_3.dtd">
<module name="Checker">
    <property name="charset" value="UTF-8"/>

    <!-- 阿里规范：单行不超过 120 个字符；package/import 行和长 URL 可例外。 -->
    <module name="LineLength">
        <property name="max" value="120"/>
        <property name="ignorePattern" value="^(package|import) .*|^.*https?://.*$"/>
    </module>

    <module name="SuppressionFilter">
        <property name="file" value="${checkstyle.suppressions.file}"/>
    </module>

    <!-- Spring AOP uses subclass proxies in this application; a final target cannot be proxied. -->
    <module name="RegexpMultiline">
        <property name="id" value="FinalAopProxiedClass"/>
        <property name="fileExtensions" value="java"/>
        <property name="format"
                  value="(?s)\bfinal\s+class\b.{0,20000}?@(Transactional|Async|Scheduled|Cacheable|CacheEvict|CachePut|Caching|Validated|Retryable|CircuitBreaker|RateLimiter|TimeLimiter|Bulkhead|PreAuthorize|PostAuthorize|PreFilter|PostFilter|Secured|RolesAllowed)\b"/>
        <property name="maximum" value="0"/>
        <property name="message"
                  value="A final class must not declare an AOP-triggering annotation; Spring cannot create its CGLIB proxy."/>
    </module>

    <module name="TreeWalker">
        <!-- 阿里规范：类、方法、变量和常量使用可读且一致的 Java 命名。 -->
        <module name="TypeName"/>
        <module name="MethodName"/>
        <module name="MemberName"/>
        <module name="ParameterName"/>
        <module name="LocalVariableName"/>
        <module name="ConstantName"/>

        <!-- 阿里规范：禁止通配、重复和未使用 import。 -->
        <module name="AvoidStarImport"/>
        <module name="RedundantImport"/>
        <module name="UnusedImports"/>

        <!-- 阿里规范：modifier 顺序固定且不重复。 -->
        <module name="ModifierOrder"/>
        <module name="RedundantModifier"/>

        <!-- 阿里规范：大括号、空白和一行一语句保持一致。 -->
        <module name="LeftCurly"/>
        <module name="RightCurly"/>
        <module name="NeedBraces"/>
        <module name="WhitespaceAfter"/>
        <module name="WhitespaceAround"/>
        <module name="EmptyLineSeparator"/>
        <module name="OneStatementPerLine"/>
        <module name="MultipleVariableDeclarations"/>
        <module name="EmptyStatement"/>

        <!-- 阿里规范：switch 必须显式处理 fall-through；equals/hashCode 必须成对实现。 -->
        <module name="FallThrough"/>
        <module name="EqualsHashCode"/>
    </module>
</module>
